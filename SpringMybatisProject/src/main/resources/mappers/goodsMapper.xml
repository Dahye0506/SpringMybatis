<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mappers.goodsMapper">

<!-- result는 일반컬럼 -->
<resultMap type="Model.GoodsDTO" id="baseResult">
	<id column="PROD_NUM" jdbcType="BIGINT" property="prodNum"/>
	<result column="PROD_NAME" jdbcType="VARCHAR" property="prodName"/>
	<result column="PROD_PRICE" javaType="BIGINT" property="prodPrice" />
	<result column="PROD_IMAGE" javaType="VARCHAR" property="prodImage" />
	<result column="PROD_DETAIL" javaType="VARCHAR" property="prodDetail" />
	<result column="PROD_CAPACITY" javaType="BIGINT" property="prodCapacity" />
	<result column="PROD_SUPPLYER" javaType="VARCHAR" property="prodSupplyer" />
	<result column="PROD_DEL_FEE" javaType="BIGINT" property="prodDelFee" />
	<result column="RECOMMEND" javaType="VARCHAR" property="recommend" />
	<result column="EMPLOYEE_ID" javaType="VARCHAR" property="employeeId" />
	<result column="CTGR" javaType="VARCHAR" property="ctgr" />
</resultMap>

<select id="goodsNum" resultType="string">
	select PROD_SEQ.nextval from dual
</select>

<insert id="goodsWrite" parameterType="Model.GoodsDTO" >
	insert into products(PROD_NUM, PROD_NAME, PROD_PRICE,
		PROD_DETAIL, PROD_CAPACITY, PROD_SUPPLYER, PROD_DEL_FEE,
		RECOMMEND, EMPLOYEE_ID, CTGR, PROD_IMAGE)
	values (#{prodNum},#{prodName},#{prodPrice},
			#{prodDetail},#{prodCapacity},#{prodSupplyer},#{prodDelFee},
			#{recommend},#{employeeId},#{ctgr},#{prodImage})
</insert>


<select id="goodsList" resultType="gd">
	select PROD_NUM prodNum, PROD_NAME prodName, PROD_PRICE prodPrice, 
		PROD_IMAGE prodImage, PROD_DETAIL prodDetail, PROD_CAPACITY prodCapacity,
		PROD_SUPPLYER prodSupplyer, PROD_DEL_FEE prodDelFee, RECOMMEND recommend,
		EMPLOYEE_ID employeeId, CTGR ctgr
	from products

</select>
<!-- id 는 primary key -->


<select id="goodsDetail" parameterType="string" resultMap="baseResult">
	select PROD_NUM, PROD_NAME, PROD_PRICE, 
		PROD_IMAGE, PROD_DETAIL, PROD_CAPACITY,
		PROD_SUPPLYER, PROD_DEL_FEE, RECOMMEND,
		EMPLOYEE_ID, CTGR
	from products
	where PROD_NUM = #{prodNum}
</select>



</mapper>